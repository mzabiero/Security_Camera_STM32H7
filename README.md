# Security_Camera_STM32H7
Project implemented on the WeAct stm32h723vg6 board using a camera to detect moving target in certain frame, and if any unwanted target is detected, system calls user using its bluetooth module. System can also save recent frames to a file, and display them on the lcd display.
